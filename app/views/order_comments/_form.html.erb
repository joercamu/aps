<%= form_for([@order,@comment], remote:true, html:{id:"form-comments",:"data-type"=>"json"}) do |f|%>
	<%= f.text_area :body,required:true,placeholder:"Escribir observacion."%>
	<%= f.submit "Insertar", class:"button expand"%>
<%end%>